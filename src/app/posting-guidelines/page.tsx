import { PostingGuidelinesContent } from "@/components/PostingGuidelinesContent";

export const metadata = {
  title: "Posting guidelines for employers",
  description:
    "How to prepare listings that are clear for candidates and faster for administrators to review on TürkiyeJobs.org.",
};

export default function PostingGuidelinesPage() {
  return <PostingGuidelinesContent />;
}
